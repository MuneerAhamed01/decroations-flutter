import 'package:flutter/material.dart';

class AnimatedCrossOverEg extends StatelessWidget {
  const AnimatedCrossOverEg({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
